{-# LANGUAGE NoImplicitPrelude, ForeignFunctionInterface,
  MultiParamTypeClasses, NondecreasingIndentation, ExplicitForAll,
  PatternGuards #-}
module F7794241860471296499 where
import Foreign.C.Types (CInt)
import GHC.Types (IO)
import Foreign.C.Types (CInt(CInt))
import GHC.Types (IO(IO))
import Foreign.C.Types (CSize(CSize))
import System.Posix.Types (COff(COff))

unset_icanon :: CInt -> IO ()